Flask==3.0.0
pymumble==1.6.1
yt-dlp
ffmpeg-python==0.2.0
gunicorn

# Matrix Bot and WebRTC Audio (MatrixRTC via LiveKit)
matrix-nio[e2e]
livekit
livekit-api
PyYAML

# opuslib (via pymumble) hard-pins protobuf==3.12.2, but livekit-protocol's
# generated code needs google.protobuf.internal.builder (protobuf >=4.21).
# We therefore force protobuf 4.x in the Dockerfile after the main install,
# and install opuslib with --no-deps to skip its conflicting pin.
protobuf>=4.21,<5
six
cffi
