Flask==3.0.0
pymumble==1.6.1
yt-dlp
ffmpeg-python==0.2.0
gunicorn

# Matrix Bot and WebRTC Audio (MatrixRTC via LiveKit)
matrix-nio[e2e]
livekit
livekit-api
PyYAML

# NOTE: pymumble/opuslib hard-pin protobuf==3.12.2, but livekit-protocol
# needs protobuf >=4.21 (google.protobuf.internal.builder). The conflict is
# resolved in the Dockerfile: pymumble/opuslib are installed with --no-deps
# and protobuf is force-upgraded to 4.x afterwards.
six
cffi
